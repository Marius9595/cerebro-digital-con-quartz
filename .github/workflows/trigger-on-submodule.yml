name: Trigger Quartz build when submodule updates

on:
  repository_dispatch:
    types: [content_updated]

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Received content_updated dispatch, main deploy workflow will run."
